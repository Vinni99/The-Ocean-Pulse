// add routes for
const db = require("../models");

//accessing the db for the locals
db.local.create({
	//adding database with sequalize?
});

//signup route

//login route

//logout route
appendFile.get;
